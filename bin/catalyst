#!/usr/bin/env node

/**
 * Catalyst CLI Entry Point
 * Loads the compiled TypeScript CLI from dist/cli/index.js
 */

import('../dist/cli/index.js').catch((err) => {
  console.error('Error loading Catalyst CLI:', err.message);
  process.exit(1);
});
